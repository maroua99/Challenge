let message = "There is no war in Ba Sing Se";

//split the phrase in an array
let wordsArray = message.split(" ");

let numberOfWords = wordsArray.length;

console.log(`The message contains ${numberOfWords} words.`);
